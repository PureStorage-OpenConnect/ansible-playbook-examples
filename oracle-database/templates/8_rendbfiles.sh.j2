source ~/.bash_profile
export ORACLE_SID={{ tgt_db }}
export ORACLE_HOME={{ tgt_oh }}
export PATH=$PATH:$ORACLE_HOME/bin

sqlplus / as sysdba <<EOF
startup mount pfile='{{ new_init }}'
@rename_files.sql
alter database open;
EOF
